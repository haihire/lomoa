# 환경변수 (절대 커밋 금지)
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# 에디터
.vscode/
.idea/

# Docker 볼륨 / SSL 인증서
nginx/ssl/
*.pem

# 로컬 이미지 변경 산출물
이미지변화/

# Python 크롤러
crawlers/
crawlers/__pycache__/
crawlers/*.pyc
crawlers/.venv/

# Redis
dump.rdb

# 로컬 스크립트 (개발 환경 전용)
scripts/

# 로컬 개발 오버라이드 (EC2에는 불필요)
docker-compose.override.yml

# 로그
*.log
server-stdout.log
server-stderr.log
server/logs/
client/logs/
crawlers/logs/

# site-finder 크롤 산출물 (JSON, 디버그 HTML)
site-finder/output/

# TypeScript 빌드 캐시
server/tsconfig.tsbuildinfo
server/tsconfig.build.tsbuildinfo

# Prisma generated client (빌드 시 prisma generate로 재생성 — prebuild)
server/generated/

# 이력서 (로컬 개인정보)
이력서_개선안.md
server/dist/

# 개인 노트 / AI 대화 기록 / 포폴 초안 (로컬 전용)
docs/private/
docs/*
notes/

# AI 컨텍스트 문서 (로컬 전용 — 민감정보 포함)
context/

# 공개 문서 예외
!docs/architecture.drawio
!docs/architecture.svg
!docs/performance.md

# 에이전트 지침서 (로컬 운영용)
.github/copilot-instructions.md
docs/HARNESS.md

자주쓰는명령어.md