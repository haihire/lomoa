# 환경변수 (절대 커밋 금지)
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# 에디터
.vscode/
.idea/

# Docker 볼륨 / SSL 인증서
nginx/ssl/
daloa-key.pem

# 로컬 이미지 변경 산출물
이미지변화/

# Python 크롤러
crawlers/
crawlers/__pycache__/
crawlers/*.pyc
crawlers/.venv/

# Redis
dump.rdb

# DB 덤프 파일
scripts/dump.sql
scripts/node_modules/
scripts/package.json
scripts/package-lock.json
scripts/seed-sites.ps1

# 로컬 개발 오버라이드 (EC2에는 불필요)
docker-compose.override.yml

# 로그
*.log
server-stdout.log
server-stderr.log
server/logs/
client/logs/
crawlers/logs/

# TypeScript 빌드 캐시
server/tsconfig.tsbuildinfo
server/tsconfig.build.tsbuildinfo

# 이력서 (로컬 개인정보)
이력서_개선안.md
server/dist/
